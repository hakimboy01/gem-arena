import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hakimboy.junglecoinrush',
  appName: 'Jungle Coin Rush',
  webDir: '.',
  server: { androidScheme: 'https' }
};

export default config;
