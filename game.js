const symbols=[['🐯','WILD'],['🎯','SCATTER'],['🐾','GOLDEN CLAW'],['🍃','JADE LEAF'],['💎','RUBY'],['🔷','SAPPHIRE'],['✦','GOLD']];
let credits=5000,bet=50,spinning=false,speed='slow',ads=0,sound=true,volume=.7;
const $=s=>document.querySelector(s);
function make(){return Array.from({length:5},()=>Array.from({length:3},()=>symbols[Math.floor(Math.random()*symbols.length)]))}
let grid=make();
function render(g,spin=false){$('#reels').innerHTML=g.map((r,i)=>'<div class="reel">'+r.map((x,j)=>'<div class="symbol '+(x[1]==='WILD'?'wild':x[1]==='SCATTER'?'scatter':'')+'" style="--delay:'+(i*80+j*30)+'ms">'+x[0]+'<small>'+x[1]+'</small></div>').join('')+'</div>').join('');$('#reels').classList.toggle('spinning',spin);$('#reels').style.setProperty('--duration',{slow:'2.3s',normal:'1.7s',fast:'1.15s','2x':'.76s'}[speed])}
function beep(f,d=.08){if(!sound)return;let C=window.AudioContext||window.webkitAudioContext;if(!C)return;let c=new C,o=c.createOscillator(),g=c.createGain();o.frequency.value=f;o.connect(g);g.connect(c.destination);g.gain.value=.12*volume;o.start();g.gain.exponentialRampToValueAtTime(.001,c.currentTime+d);o.stop(c.currentTime+d)}
render(grid);
$('#spin').onclick=()=>{if(spinning||credits<bet)return;spinning=true;credits-=bet;$('#credits').textContent=credits.toLocaleString();$('#message').textContent='SPINNING • GOOD LUCK';beep(220);grid=make();render(grid,true);let d={slow:2300,normal:1700,fast:1150,'2x':760}[speed];setTimeout(()=>{spinning=false;$('#reels').classList.remove('spinning');let wild=grid.flat().filter(x=>x[1]==='WILD').length;let win=wild>=3?270:Math.random()<.38?Math.floor(20+Math.random()*100):0;credits+=win;$('#credits').textContent=credits.toLocaleString();$('#win').textContent=win.toLocaleString();$('#message').textContent=win?'WIN +'+win+' CREDITS':'NO WIN • TRY AGAIN';beep(win?660:150,.16)},d)};
$('#minus').onclick=()=>{bet=Math.max(10,bet-25);$('#bet').textContent=bet};$('#plus').onclick=()=>{bet=Math.min(1000,bet+25);$('#bet').textContent=bet};
document.querySelectorAll('[data-speed]').forEach(b=>b.onclick=()=>{speed=b.dataset.speed;document.querySelectorAll('[data-speed]').forEach(x=>x.classList.toggle('active',x===b));beep(420,.04)});
$('#settings').onclick=()=>$('#panel').classList.toggle('open');$('#sound').onchange=e=>sound=e.target.checked;$('#volume').oninput=e=>volume=e.target.value/100;
$('#free').onclick=()=>$('#message').textContent='FREE SPIN BONUS READY • DEMO';$('#ad').onclick=()=>{if(ads>=5){$('#message').textContent='AD LIMIT 5/5';return}ads++;$('#ads').textContent=ads;credits+=100;$('#credits').textContent=credits.toLocaleString();$('#message').textContent='REWARDED AD DEMO • +100 CREDITS';beep(880,.12)};
